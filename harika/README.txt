hellow every one
I have done the project for restuarant management system using NetBeans ide
 + 
 + 
 + HOW TO RUN:
 + open NetBeans>file>new project>java>java application
 + it creates a pom.xml file
 + select it and  run as maven build
 + once build completes refresh the project
 + it creates a jar file in target location
 +OR
 +open NetBeans>file>export>java>jar file
 +select the source files and required files
 +it creates a jar file in specified location
 +
